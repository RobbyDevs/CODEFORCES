import sys

input = sys.stdin.readline


def solve():
    a = input().strip()
    b = input().strip()

    
    ans = len(a)+len(b)
    aux = 0
    
    for i in range(len(b)):
        aux = i
        
        for j in range(len(a)):
            if (i<len(b))
    
for w in range(int(input())):
    solve()
    